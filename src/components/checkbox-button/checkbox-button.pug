mixin checkbox-button(options)
  -var checkLabel = options.label
  .checkbox-button
    label
      input(class='checkbox-button__input', type='checkbox', name=options.name, value=options.value)
      span(class='checkbox-button__title') #{checkLabel}
